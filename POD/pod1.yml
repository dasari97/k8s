appVersion: v1
kind: ConfigMap
metadata:
  name: sample_configmap
data: 
  url: google.com

---
appVersion: v1
kind: Pod
metadata:
  name: pod1
specs:
  containers:
    - name: centos
      image: centos:7
      command:
        - "sleep"
        - "1000"
      envFrom:
      - configMapKeyRef:
          name: url
          key:  url
          